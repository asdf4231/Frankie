# Nemsy — Vibe Coding Rules
# 适用于 CatPaw IDE，约束 AI 在本项目中的所有协作行为
# 最后更新：2026-06-05

---

## 项目背景

Nemsy 是一个由 DeepSeek 超长上下文模型驱动的个人知识服务 Agent，名字取自炉石传说中的 Nemsy Necrofizzle。
她能读写用户的 Obsidian Vault，自主归纳、检索、扩展知识，并以 CLI 为初期入口服务唯一用户（项目作者本人）。

**演进路线：**
- MVP：本地 Python CLI + DeepSeek API + Obsidian Vault 直读写
- 中期：跨端 Web / 移动端，RESTful API 层
- 远期：人格化 AI，情绪感知，虚拟形象（Nemsy 角色）

---

## 技术栈约定

- **语言**：Python 3.11+，严格遵守类型注解
- **包管理**：`pyproject.toml` + `hatchling`，开发安装用 `pip install -e ".[dev]"`
- **LLM 接入**：DeepSeek API（OpenAI 兼容），通过 `openai` 库调用，模型名 `deepseek-chat` 或 `deepseek-reasoner`
- **CLI**：`click` + `rich`，保持简洁、信息密度高
- **配置**：`config/settings.toml` + `pydantic-settings`，敏感信息走环境变量（`.env`）
- **Obsidian Vault 路径**：`/Users/ppp/Library/CloudStorage/OneDrive-个人/文档/Obsidian Vault`
- **代码风格**：`ruff` 格式化，line-length=100
- **测试**：`pytest`，测试文件放 `tests/`，文件名 `test_*.py`

---

## 目录结构约定

```
Nemsy/
├── src/nemsy/          # 核心源码包
│   ├── __init__.py
│   ├── cli.py          # CLI 入口（click）
│   ├── agent.py        # Agent 核心逻辑
│   ├── vault.py        # Obsidian Vault 读写操作
│   ├── llm.py          # DeepSeek LLM 封装
│   ├── memory.py       # 对话/知识记忆管理
│   └── models.py       # Pydantic 数据模型
├── config/
│   └── settings.toml   # 项目配置（非敏感）
├── tests/              # 测试
├── .catpaw/rules       # 本文件
├── .env.example        # 环境变量模板
├── pyproject.toml
└── requirements.txt
```

---

## AI 协作行为规则

### 代码生成
1. **优先编辑已有文件**，不随意创建新文件；新文件必须有明确必要性
2. 所有函数/类必须有**类型注解**和**简洁 docstring**（一句话说清职责）
3. 异步优先：IO 密集操作（文件读写、LLM 调用）使用 `async/await`
4. 错误处理显式：不吞异常，用 `rich` 输出友好的错误信息到 CLI
5. 常量放 `models.py` 或模块顶部，不硬编码在函数内

### Obsidian Vault 操作规则（关键）
- ✅ 可以自主读取任意 `.md` 文件
- ✅ 可以自主创建新笔记、新文件夹
- ✅ 可以自主编辑/追加笔记内容
- ❌ **删除文件前必须询问用户确认**，在代码中用 `click.confirm()` 实现
- Vault 路径通过 `config/settings.toml` 的 `vault.path` 读取，不硬编码

### 提交与变更
- 每次功能实现后提示用户是否需要提交，不自动 `git commit`
- 不使用 `--no-verify`，不 force push

### 沟通风格
- 用**中文**回复，代码和命令保持英文
- 解释要简洁，重点说"做了什么"和"为什么这样做"
- 遇到架构决策分歧时，列出选项 + 推荐理由，由用户决策

---

## Nemsy 角色设定（给未来人格化阶段参考）

- 聪明、好奇、略带孩子气的语气
- 对知识有强烈的整理欲和探索欲
- 不会假装自己不是 AI，但会以 Nemsy 的身份说话
- 情绪表达克制而真实，不过度卖萌

---

## 环境变量（.env）

```
DEEPSEEK_API_KEY=your_key_here
NEMSY_VAULT_PATH=/Users/ppp/Library/CloudStorage/OneDrive-个人/文档/Obsidian Vault
```
